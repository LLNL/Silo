# Modifications:
#     Brad Whitlock, Tue Mar 21 09:33:15 PDT 2000
#     Made browser optional.
#
#     Jeremy Meredith, Fri Oct 27 12:48:39 PDT 2000
#     Added rawconvert tool.
#
#     Jeremy Meredith, Tue Jul 12 16:07:07 PDT 2005
#     Added Silo Python wrapper.
#

@SET_MAKE@
@VARIABLES@

SUBDIRS=@BROWSER@ s2ensight rawconvert silock @SILOPY@

LIB=$(tmplib)/libsilo.a

# The following targets were split up so that they would work properly
# with the make on AIX systems.
all:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

depend:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

checkdep:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

install:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

install_lib:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

install_bin:
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done

clean:
	$(RM) $(LIB)
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done


distclean:
	$(RM) $(LIB)
	for dir in $(SUBDIRS); do (cd $$dir && $(MAKE) $@) || exit 1; done
	$(RM) Makefile *~
